import { loginSchema, registerFormSchema, type LoginInput, type RegisterFormData } from "@sentinel/shared-schemas";

export { loginSchema, registerFormSchema };
export type { LoginInput, RegisterFormData } 